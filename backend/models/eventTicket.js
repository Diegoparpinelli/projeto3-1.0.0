

class eventTicketSchema {
        
    constructor(enable, type, ps) {
        config: {
            this.enable = enable 
            this.type = type
            this.ps = ps
        }
        
    }
 
}

module.exports = eventTicketSchema;

